import * as React from 'react';
import * as ReactDOM from 'react-dom';

document.addEventListener('DOMContentLoaded', () => {
    ReactDOM.render(<div>Keks!</div>, document.getElementById('ctf-root'));
});
